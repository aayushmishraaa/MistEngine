c++
#include "RigidBody.h"

// No implementation needed in the .cpp file for now, as the update function is in the header (for simplicity of this example).
// If the update function becomes more complex, it should be moved here.